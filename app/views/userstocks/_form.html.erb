<%= form_for(userstock) do |f| %>
  <% if userstock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userstock.errors.count, "error") %> prohibited this userstock from being saved:</h2>

      <ul>
      <% userstock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :stock_id %>
    <%= f.number_field :stock_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
